# AGENTS.md

本文件约束在本仓库中工作的编码 Agent（智能体）。项目已进入逐功能敏捷开发，当前状态以 MVP 产品规格与对应迭代文档为准；不要把已经确认的产品能力或 starter 继承草稿描述成已经实现。

## 事实来源

发生冲突时按以下顺序解释项目事实：

1. 用户在当前任务中的明确指示；
2. `docs/mvp-product-spec.md` 中已经由用户确认的 MVP 范围、依赖层、当前排序和实施状态；
3. `docs/product-capability-register.md` 的当前基础结论和能力表；
4. 同一文件中日期更晚的决策记录；
5. `CONTEXT.md` 的领域术语；
6. 外部产品或技术调研仅作为证据，不自动成为本项目需求。

历史记录用于保存决策演变，不代表当前范围；不能因为早期记录与当前能力表不同，就恢复已经被后续决定修改的方案。

## 核心产品边界

- 核心价值是多 Markdown 文档的优雅查看和编辑；
- 性能是一等指标；不提前冻结数值门禁或预期目标，在相关功能迭代中测量、记录并比较真实表现；
- 用户本地 `.md` 与 `.markdown` 文件是正文唯一真源，App（应用）的核心功能必须离线可用；
- MVP（Minimum Viable Product，最小可行产品）为 macOS-first（首先支持 macOS），Agentic（智能体化）能力不进入 MVP；
- 不提供完整 WYSIWYG（What You See Is What You Get，所见即所得）编辑模型；
- 不支持 `.mdx`，不允许 Markdown 内容触发 shell（命令行解释器）、脚本、任意进程或越权文件访问；
- 已确认的产品行为与候选实现必须分开表达，技术候选不等于最终选型。

## 领域语言

涉及文档身份、路径、保存、恢复或解析语义前，先阅读 `CONTEXT.md`。尤其不要混淆：

- `documentId`：App 内稳定、不透明的逻辑文档身份；
- canonical absolute path：按真实文件系统语义规范化的当前绝对路径，只负责定位、授权与去重；
- Canonical Markdown 语义：App 对 Markdown 的规范、权威解释；
- `DocumentStructureSnapshot`：供大纲、折叠和锚点消费的轻量文档结构快照，不是第三棵完整 AST（Abstract Syntax Tree，抽象语法树）；
- PoC（Proof of Concept，概念验证）：用于验证风险与可行性的实验，不是生产实现；
- M0：能力总账中原先用于正式编码前约束与技术验证的阶段；当前不再设置独立的前置 M0 阶段，必要的 PoC 随具体功能迭代即时进行。

术语表覆盖多个产品阶段；某个概念出现在 `CONTEXT.md` 中，不代表它属于 MVP。

## 决策文档维护

修改产品决定时，应同步检查并更新：

1. 如果影响 MVP 范围、依赖、排序或实施状态，更新 `docs/mvp-product-spec.md`；
2. 产品能力总账顶部的当前基础结论；
3. 对应的详细结论和能力表行；
4. 文件末尾追加的带日期决策记录；
5. 新增或改变领域概念时的 `CONTEXT.md`。

保留早期历史记录原貌，通过更新当前结论和追加较新记录表达覆盖关系，不能静默改写历史。每项决定至少说明是否提供、所属阶段、用户行为、实现边界、性能影响以及明确不做的内容。

## 技术决策纪律

- 不提前编写或冻结完整技术方案；每轮只确定当前功能所需的最小技术方案和必要边界；
- 不提前冻结性能数值、RPC（Remote Procedure Call，远程过程调用）载荷、索引后端或完整 schema（数据结构规范）；需要 benchmark（基准测试）或 PoC 时，在相关功能迭代中执行并以结果指导后续选择；
- `DocumentStructureSnapshot`、micromark/unified 等是当前方向或候选时，应明确保留开发阶段重新评估的空间；
- 不为了未来 Agentic、插件、多平台或协作能力提前扩大 MVP；
- 不虚构不存在的源码目录、安装步骤、命令、测试、构建结果或发布状态。

## 敏捷迭代方式

### 已结束的批次授权（2026-09-21）

用户已于 2026-09-21 确认“模块 1～6验证通过”，L1 方案 A 基础范围验收完成，批次授权结束；现恢复下述默认逐轮方式。后续层仍需用户选择，不自动提交或推送。

用户已选择方案 A：L1 收敛为可独立验收的基础能力，后续消费者接入责任按 MVP 规格第 6.1 节保留。该批次允许 Agent 自动选择和连续实现剩余 L1 基础工作，逐项保留最小迭代文档与验证记录，最终按模块集中人工验收，不必逐轮再次请求选择或验收。此前已验收切片保持历史结论；新改动只能记为待人工验收。本授权不包含自动提交、推送或自动进入后续层，也不允许删除后续需求。批次结束恢复下述默认逐轮方式。

- 先维护一份独立的 MVP 产品规格，列出全部 MVP 功能点及其实现优先级；
- 功能按依赖关系划分为 `L1` 至 `Ln`，层数按实际情况调整；依赖层不是必须逐层清空的阶段门禁，不要求完成整个前层才能进入后层；
- 每层内部采用动态有序 Backlog（待办列表）：初次排序后可随验证结果调整；每轮结束时由用户决定下一轮功能，Agent 不自行连续推进；
- 每轮选择功能前，先介绍当前关注层的整体规划，再分别汇报产品范围确认、功能讨论、迭代文档、实现、自动验证和人工验收的进展；关注层由用户决定，用户未指定时默认建议编号最小且仍有未完成项的层，但这个默认值不是门禁。列出该层尚未完成且前置依赖已经满足的功能，给出推荐与理由后由用户选择；用户明确要求跨层或选择尚缺依赖的功能时，应说明影响，并在当轮决定补入最小依赖、拆分切片或调整方案；
- 功能粒度按具体情况决定：可以整项实现，也可以在进入该轮时拆成多个可独立验收的切片，并用切片替换原项重新排序。切片使用稳定的子 ID，例如 `F-001a`；只有全部必要切片均已人工验收，父功能才可以标记完成；
- starter、框架或其他基线中已经存在但尚未按本产品规则讨论、测试和人工验收的能力统一标记为“继承草稿”。继承草稿可以作为后续实现起点，但代码存在、界面可见或基础接线可用都不代表对应产品功能已经实现；
- 每轮在 `docs/iterations/<featureId>-<英文短名称>.md` 维护唯一一份单功能迭代文档。开发前只要求确认功能说明、本轮技术方案和最小 happy path（正常成功路径）测试；文档确认后才开始开发；
- F-002 为持续约束，不单独作为开发轮次或其他功能的完成前置；涉及它的迭代须记录本轮落实方式和真实验证证据，不以规则确认替代实现验收；
- 持续维护 `docs/product-feature-manual.md`（产品功能手册）：每轮新增或变更功能，都同步记录使用方式、当前行为、限制、关联功能 ID 和迭代记录；实现后标注待人工验收，通过后更新为已验收。保留简短变更记录，尚未实现的规划与继承草稿不得写成可用功能。手册描述实际产品，不代替产品规格、迭代方案或验收证据；
- 实现中若需要改变已确认的产品行为、范围或关键技术路线，先更新同一份迭代文档并重新取得用户确认；不要另建一份总结文档替代原记录；
- 不采用 TDD（Test-Driven Development，测试驱动开发）作为默认模式；测试可在实现期间或实现后补充，但必须真实验证本轮约定的最小 happy path；
- 是否增加异常路径、安全测试、专项性能测试或临时 PoC，由当前功能的风险和当轮讨论决定，不提前为所有功能统一规定；
- 实现后在迭代文档中追加简短的实际改动、真实自动验证命令及结果；实现和自动验证完成只表示进入人工验收；
- 用户明确确认通过后，记录人工验收结论和日期，更新切片、父功能与依赖层状态。本轮提交和 push 必须分别获得明确授权，人工验收通过不自动授权 Git 操作；本轮关闭后再由用户决定下一项。

## 工作与验证

### 开发包 alpha 版本号（2026-09-22 起）

- 每次修改代码后重新打包，必须递增 alpha（开发预发布）版本号，格式为 `<基础版本>-alpha.<序号>`；同一基础版本下序号单调递增。当前版本为 `0.1.0`，首次按本规则打包使用 `0.1.0-alpha.1`，之后为 `0.1.0-alpha.2`，依此类推。
- 为确保新版本号真正进入产物，应在打包前更新版本唯一来源 `apps/desktop/package.json` 的 `version`；打包后核对产物版本与源配置一致，并在当轮迭代文档记录版本、构建命令和结果。不能仅在打包后改源码版本、仍交付旧版本产物；失败构建不得记为新包已交付。
- alpha 是应用版本的预发布标识，不改变现有 `canary` 构建渠道，不自动触发 release（发布）、Git tag（版本标签）、提交或推送。仅修改文档且不打包时无需递增；正式发布的基础版本与发布流程另行确认。

### 迭代检查与执行

- 每轮启动前读取 `docs/deferred-obligations.md`，列出所选功能的全部主承接 ID；在迭代中用 `<!-- obligations: OBL-xxx, OBL-yyy -->` 声明，无项写 `<!-- obligations: none -->`。切片分工可分别承接，但父功能的剩余项仍须显式保留；不要只依赖历史对话或 6.1 概览。
- 每轮启动与收尾必须运行 `bun run check:obligations`（亦接入 `check:feature-docs`）。收尾逐项核对实现、验证及用户人工验收证据；迁出项只有明确验收才关闭，主承接已验收但遗留项未关闭必须阻止完成标记。改派需同步登记与迭代并追加变更说明，不删除 ID。
- 切片只承接部分时，未承接 ID 在 `<!-- deferred-obligations: OBL-xxx -->` 显式列出，并说明后续切片与理由；没有则写 none。延期不等于取消，登记保持待承接，父功能不得提前已验收。
- 每轮文档显式填写“继承约束检查”：F-002、中文与外观、命令与键盘、安全与日志四项。逐项说明适用行为和验证方式，不适用须写理由；新 UI 与新模块仍继承中文、浅深/系统主题、统一命令上下文、键盘等价入口及匿名日志要求。脚本只能核对结构和引用，不能代替语义评审或人工验收。

- 本项目的开发、构建和运行命令在工具沙箱外执行；工具需要时使用相应权限申请，不以沙箱内失败判定应用故障。离线验证所需的临时网络限制与日常运行分开记录。
- 搜索文件和文本优先使用 `rg` 或 `rg --files`；
- 修改文件使用 `apply_patch`，保留用户和其他任务的无关改动；
- 文档修改至少运行 `git diff --check`，并核对内部链接、README 中英文事实等义、能力 ID 重复和 Markdown 表格结构；
- 代码出现后，按受影响范围运行真实的格式检查、类型检查、测试和构建，不用其他检查替代；
- 未经用户明确要求，不创建提交、不 push、不改写 Git 历史；
- 用户要求提交时只暂存任务相关文件，提交前复核 staged diff（暂存差异）；提交不代表允许 push。
