# 产品功能手册

本手册用于记录 Agentic Markdown 当前实际可用的功能与操作方式，随每轮迭代维护；不把产品规划或已有草稿当作已交付能力。

范围与优先级见 [MVP 产品规格](mvp-product-spec.md)，实现、验证与人工验收证据见[迭代记录](iterations/README.md)。本手册不替代上述文档，也不另行定义产品需求。

## 当前状态

2026-09-22 更新：F-012a 其他项目用户已验收通过；选区显示问题已在 alpha.8 修复并通过自动与界面验证，仅该项待用户复验，整体仍待人工验收。没有保存能力，不要用于需要保留修改的真实工作；本轮行为见下方“待验收功能”，部分通过不代表整体完成。

2026-09-22 更新：用户确认 Electrobun 2.0.1 迁移包 `0.1.0-alpha.4` **人工回归验收通过**，归属 [F-012a 前置迁移](iterations/F-012a-memory-editing-foundation.md)。当前读取、标签及快捷键用法不变，仍无编辑或保存；官方关闭确认仅在隔离程序验证，不是主应用已交付的未保存保护。仅记录用户结论，不追加具体操作证据。

2026-09-22 更新：F-011 常驻文档标签已通过用户人工验收，验收包 `0.1.0-alpha.3`；当前仍为只读原文，后续编辑、模式与稳定锚点、恢复及缓存能力不随本轮验收完成。

2026-09-22 更新：F-009a 欢迎页与独立文件列表已通过用户人工验收；F-009 父功能仍部分完成，文件夹与最近项入口继续由 OBL-039 / OBL-040 追踪，未随切片验收关闭。

2026-09-21 批次更新：用户确认“模块 1～6验证通过”，按方案 A 收敛后的 L1 基础能力已 [集中验收通过](iterations/L1-module-acceptance.md)。下文既有切片中“完整父功能未完成”指重划前范围；后续能力转由 [规格 6.1](mvp-product-spec.md)列出的消费者实现，不视为已交付。F-008a 的使用与限制见下方已验收功能。

截至 2026-09-21，F-001 macOS 可运行应用外壳和 F-003a 只读文档服务验证入口已验收。当前仍是开发中的验证界面，还不能作为正式 Markdown 阅读器或编辑器使用，尚未正式发布。

F-004a 单文件路径识别与只读授权已于 2026-09-20 通过人工验收，详见下方已验收功能；完整 F-004 仍为部分完成。

F-006a 现有操作的统一命令与键盘入口已于 2026-09-20 通过人工验收；详见下方已验收功能，完整 F-006 仍为部分完成。

F-007a 现有界面简体中文与 App 外观已于 2026-09-21 通过人工验收，详见下方已验收功能；完整 F-007 仍为部分完成。

[F-005a：现有文档读取任务的取消与有界调度](iterations/F-005a-document-task-cancellation.md)已于 2026-09-21 通过人工验收，详见下方已验收功能；完整 F-005 仍为部分完成。

starter（初始项目模板）附带能力除下方已验收或待验收的明确范围外，仍为**继承草稿**：保留已有代码和入口，但尚未按本产品规则测试或验收，不作可用性承诺。强调色、更新等业务不因本轮翻译而升级为已验收。

## 已验收功能

### F-001：应用启动与窗口基本操作

| 操作 | 当前行为 |
| --- | --- |
| 启动应用 | 在已验证的 macOS 环境中打开本地构建的 Agentic Markdown，显示侧栏和正文区域；正文现由下述 F-003a 验证入口替代占位内容。 |
| 移动窗口 | 拖动窗口红黄绿按钮右侧或正文顶部的空白区域，可连续移动窗口，无需反复切换焦点。 |
| 调整布局 | 可调整窗口大小、拖动左右面板之间的分隔线；面板与分隔线延伸至窗口顶部。 |
| 显示或隐藏侧栏 | 按 `⌘B` 切换侧栏，再按一次恢复显示。 |
| 离线启动 | 已准备好的本地构建无需开发服务器，断网后退出并重新打开仍可显示外壳、使用 `⌘B` 切换侧栏。 |

当前边界：

- 面板布局不代表文件树或正式主副文档区域已经实现。
- 下述 F-003a 仅提供只读验证入口，尚未提供正式 Markdown 打开、阅读模式、编辑或保存流程；不能据外壳离线启动推断这些后续流程已离线可用。
- 当前保留临时图标；固定中文界面与 App 外观的本轮变更见下方已验收 F-007a。测试渠道的系统应用名称可能带 `-canary` 后缀。
- 本轮使用本地未签名构建，不包含正式分发、签名、公证或自动更新验收。

人工验收日期：2026-09-19。详细环境、验证结果与交互修复见 [F-001 迭代记录](iterations/F-001-app-shell.md)。

### F-003a：只读文档服务验证入口

已于 2026-09-20 通过人工验收。正文区显示“文档服务验证”；这不是正式阅读/编辑模式，F-003 整体也未完成。

| 操作 | 当前行为 |
| --- | --- |
| 选择 Markdown 文件 | 通过系统选择器选一个本地 `.md` 或 `.markdown` 文件，只读显示原文；Markdown 与 HTML 不渲染、不执行。取消或选择失败保留原显示。 |
| 核对身份与版本 | 文件名、`documentId` 与 `revision` 显示在原文上方。同一文件对象未变时重复读取保留身份；发现原地内容变化时版本增加。 |
| 重新读取 | 主动读取当前文件，不自动监听外部变化；文件被替换、删除或读取失败时提示，并保留明确标为旧快照的原内容。 |
| 清空 | 清除显示并释放只读授权；重新选择仍可恢复本次运行内已知文件对象的身份。 |

当前限制：

- 本轮验证入口仅接受有效 UTF-8、至多 1 MiB 的普通 Markdown 文件；超限失败，不截断。这不是最终产品容量承诺。
- 身份仅在本次运行内维护；不保证重命名、移动或重启后延续。编辑器若采用原子替换保存，需要重新选择文件。
- 受 Electrobun 1.18.1 选择器路径编码限制，含逗号的路径暂时安全拒绝，不把拆分出的路径片段用于读取。
- 页面耗时为本次请求等待时间，选择操作包含人在对话框中的停留时间，不作为性能基准。
- 2026-09-20 用户已确认基础交互、断网读取及外部修改后文件替换分支通过；原地修改递增版本分支有自动测试覆盖，未另记为人工操作通过。自动 UI 锁屏记录不追改为自动通过。

验收样本见 [F-003a-sample.md](iterations/fixtures/F-003a-sample.md)，方案、自动检查与人工验收步骤见 [F-003a 迭代记录](iterations/F-003a-document-read-service.md)。

### F-004a：单文件路径识别与只读授权

已于 2026-09-20 通过人工验收。本轮沿用 F-003a 的选择、重新读取和清空入口；只读原文、1 MiB、UTF-8、含逗号路径安全拒绝等限制不变。界面标识更新为 F-004a，新增“仅当前文件只读，不授权父目录或相邻文件”的权限说明。

| 操作或场景 | 本轮新增或强化行为 |
| --- | --- |
| 不同路径写法选择同一文件 | 按真实磁盘规则识别大小写和 Unicode 写法，实际指向同一文件时复用会话身份；真实不同的文件不合并，不修改磁盘文件名。 |
| 明确选择文件符号链接 | 只授权其解析到的单个 Markdown 文件，显示真实目标文件名；不会因此扫描或授权父目录、相邻文件。系统选择器可能先自行解析链接，实际校验以其返回入口为准。 |
| 入口或目标发生变化后重新读取 | 选择入口链接改向、目标或已记录的父目录被替换时拒绝旧授权，提示重新选择并保留明确标为旧快照的原文。 |
| 同目录新增或修改其他文件 | 不因此撤销当前文件授权，也不会自动获得其他文件的读取权限。 |
| 清空、切换文件或退出 | 延续上轮行为：清空、成功切换及退出撤销对应读取授权；取消或选择失败不丢弃原显示。 |

本轮仍不支持目录根授权、资产读取、写入、重命名移动跟踪或跨重启身份恢复。不同规范路径的硬链接暂不合并；只读描述符与前后检查不构成抵御所有瞬时文件替换竞态的保证。

自动验证已通过，用户已明确确认本轮验收通过。窗口内符号链接选择未完成自动验证的历史记录保持不变；自动操作中曾出现选择器长时间停留后取消导致通信失败的现象，原因尚未确认，不因人工验收通过而宣称已修复。完整方案、测试结果与验收记录见 [F-004a 迭代记录](iterations/F-004a-single-file-authorization.md)。

### F-006a：现有操作的统一命令与键盘入口

已于 2026-09-20 通过人工验收。正文只读验证区仍显示 F-004a 标识；本轮新增或统一其命令入口，不新增阅读、编辑或保存模式。

| 操作 | 入口与使用方式 |
| --- | --- |
| 选择 Markdown 文件 | 按 `⌘O`，或使用原按钮、文件菜单、命令面板，进入系统选择器；仍只读打开单个文件。 |
| 重新读取当前文件 | 使用原按钮、文件菜单或命令面板；本轮无独立快捷键。 |
| 清空当前验证内容 | 使用原按钮、文件菜单或命令面板；仅清除验证内容，不删除磁盘文件，本轮无独立快捷键。 |
| 切换侧栏 | 按 `⌘B`，或使用查看菜单、命令面板。 |
| 打开命令面板 | 按 `⌘⇧P`，或使用查看菜单；搜索命令名称，方向键选择，Enter 执行，Escape 关闭并合理恢复焦点。 |

按钮、菜单和命令面板共享可用状态，执行前也会校验：正在选择或读取时不能重复打开或重新读取；无快照时不能重新读取；有快照或忙碌时可以清空。禁用命令不会执行。清空不承诺关闭已弹出的系统选择器，过期结果不会恢复正文。

本轮产品命令使用中文名称；设置中的强调色、更新、缩放等其他业务仍保留为继承草稿，不代表完整 F-006 已验收。当前界面中文化与 App 外观已由下述 F-007a 单独验收。`⌘O`、`⌘B`、`⌘⇧P` 由 macOS 原生菜单触发，避免重复执行。

使用示例：用 `⌘O` 打开文件 → `⌘⇧P` 搜索“重新读取”并 Enter → 执行“清空”并核对重新读取不可用 → 用 `⌘B` 切换侧栏 → Escape 关闭面板并确认焦点。用户已明确确认本轮验收通过。此前真实窗口自动验证覆盖部分键盘、菜单和面板路径；搜索自动操作使用设置输入值，不作为中文输入法组合输入验证。完整方案与证据见 [F-006a 迭代记录](iterations/F-006a-command-entrypoints.md)。

### F-007a：现有界面简体中文与 App 外观

已于 2026-09-21 通过人工验收；沿用现有只读文档验证入口，不新增正式阅读、编辑或源码模式。

| 操作 | 当前实现与使用方式 |
| --- | --- |
| 查看当前界面 | App 自有菜单、侧栏、设置及命令面板固定简体中文；移除语言选择，历史英文偏好不再使界面回退英文。macOS 文件选择器仍遵循系统语言。 |
| 更改外观 | 在设置中选择浅色、深色或跟随系统；有效已有偏好保留，无偏好或无效值默认跟随系统。 |
| 退出再打开 | 外观偏好在本机保存，重启后继续应用；不保存或改写 Markdown 正文。 |
| 文档打开时切换外观 | 只改变外观，不重新读取文档；内容、身份、版本与滚动位置保持。 |
| 跟随系统 | 启动前解析外观，运行中跟随系统变化；系统读取或通知失败时保留最近有效外观。 |

开发阶段已完成真实窗口中文界面、浅深色截图、文档身份保持，以及退出重启后深色保留的检查；检查结束已恢复原来的跟随系统设置。该阶段未测量首帧闪烁和滚动保持的证据边界不变；随后用户对包含这些项目的人工验收清单明确确认通过，不追加虚构的自动测量结果。

本轮人工验收清单：核对中文界面与无语言选择 → 切换浅色、深色、跟随系统 → 打开较长文档并滚动后切换外观 → 退出重启核对偏好 → 切换 macOS 外观验证跟随系统，并观察启动是否闪烁。用户已回复“验收通过”。

强调色、更新等附带功能仍为继承草稿；第三方颜色选择器的英文辅助功能标签保留，不宣称完整 VoiceOver 验收。未来编辑/源码高亮及独立阅读主题尚未接入，父 F-007 未完成。完整方案和验证证据见 [F-007a 迭代记录](iterations/F-007a-chinese-shell-theme.md)。

### F-005a：现有文档读取任务的取消与有界调度

已于 2026-09-21 通过人工验收。沿用选择文件、重新读取和清空入口，仍非正式阅读或编辑模式；当前批次顶部标识已改为“L1 · 基础模块集中验收 · 只读原文，限 1 MiB”。

| 操作或场景 | 当前实现与使用方式 |
| --- | --- |
| 正常选择或重新读取 | 原文、权限与身份规则不变；同一未修改文件的身份和版本保持。 |
| 清空当前内容 | 界面立即清空，并通知后台取消未完成请求、释放原授权；晚到结果不能恢复已清空的内容。 |
| 连续过期读取请求 | 后台最多一个运行中的读取和一个最新待执行任务，旧任务在可取消边界停止；普通 UI 的忙碌防重复规则仍保留。 |
| 系统选择器中取消选择 | 保持原文档显示；之后可以继续选择或重新读取。后台取消不强行关闭系统选择器，选择器尚未关闭时仍拒绝第二个选择器。 |
| 取消后继续操作 | 可以重新发起读取；若系统调用尚未返回，新任务可能需要等其安全收尾。取消应答不等于底层系统调用已被中断。 |

测试覆盖了 100 个连续请求的有界调度和取消竞态，不要求人工用快速点击复现。尚无缓存系统、编辑、扫描或索引任务接入；完整 F-005 未完成。原有 1 MiB、UTF-8、单文件只读限制不变，不将此前选择器通信异常的历史观察宣称为已定位修复。

本轮人工验收清单：打开样本 → 重新读取核对身份 → 清空 → 重新打开 → 发起新选择并取消，确认原显示仍可使用；回归命令面板与侧栏。用户已回复“验收通过”，不追加未提供的操作细节或测量结果。详见 [F-005a 迭代记录](iterations/F-005a-document-task-cancellation.md)。

### F-008a：当前外壳安全、匿名日志与设置版本基线

已于 2026-09-21 随六个模块集中验收通过，包含已有外壳、读取、命令与外观的批次回归。仅记录用户确认，不追加具体操作或测量细节。

- 用 `⌘O` 打开 [安全样本](iterations/fixtures/L1-security-sample.md)：HTML、脚本、表单与链接保持原文字面显示，不能点击执行。仍不提供正式 Markdown 渲染或编辑。
- 正常退出重启保留有效设置；已有磁盘设置增加版本与迁移备份。损坏或未来版本读取默认且拒绝覆盖原件与备份，不提供自动修复入口；不要破坏个人设置来验收。
- 自有 Bun 日志保存于 App 数据目录的 `logs/app.log` 与 `app.previous.log`，各最多 256 KiB，只记录固定事件和允许的匿名数字，不记录正文、完整路径或原始异常、不上传。退出末尾日志不保证落盘；renderer 固定事件只到 console，框架内部输出不在该承诺内。
- 当前保护依赖不可信内容仅作为纯文本展示。Electrobun 原生下载策略仍有已知缺口，F-020 引入富内容前必须解决；不宣称完整原生下载沙箱已经实现。

详细证据见 [F-008a 迭代记录](iterations/F-008a-shell-security-baseline.md)，操作清单见 [L1 模块验收](iterations/L1-module-acceptance.md)。

### F-009a：欢迎页与独立文件列表

已于 2026-09-22 通过用户人工验收。此前 Agent 因 Mac 锁定未完成新窗口实测的历史记录保留；不追加用户具体操作或测量证据。证据与操作清单见 [F-009a 迭代记录](iterations/F-009a-welcome-standalone-files.md)。本轮新增行为如下，不能视为完整 F-009 或正式编辑能力已验收：

| 操作 | 本轮行为 |
| --- | --- |
| 打开空窗口 | 显示轻量中文欢迎页，可选择本地 `.md` 或 `.markdown` 文件。 |
| 打开文件 | 点击打开按钮、文件菜单或按 `⌘O`；成功后仅该文件作为侧栏顶层叶子加入，不加入或扫描父目录。 |
| 再打开另一文件 | 保留已有文件叶子，显示新文件的只读原文；点击旧叶子可重新读取并切换。 |
| 重复打开同一文件 | 根据后端规范化路径的会话位置标识去重，不增加重复叶子；该标识不是文档身份或读取授权。 |
| 区分同名文件 | 悬停文件名显示已授权路径，仅用于辨认，不授权父目录或任意路径读取。 |
| 取消或失败 | 不加入失败候选，不清空已有列表或正文；文件已被替换时需重新选择获得授权。 |
| 文档诊断信息 | 展开查看文档身份、版本和只读权限信息。 |
| 清空窗口 | 清空本轮文件集合与正文，返回欢迎页；这是验证期操作，不是正式关闭标签。 |

仍限单文件 1 MiB、有效 UTF-8、只读原文；只保留活动正文，不保存各文件正文缓存或重启恢复集合。不提供编辑、保存、持久标签、正式单击预览规则。文件夹入口和最近项仍由 F-025 / F-029 承接，F-009 整体不能随此切片自动完成。

历史 L1 截图/验收中“文档服务验证”和顶部 L1 标识保留为历史记录；本轮新界面替代该验证标题，不追改既有证据。

### F-010：编码与换行读取保真

已于 2026-09-22 通过用户人工验收，验收包为 `0.1.0-alpha.2`；证据及验收方法见 [F-010 迭代记录](iterations/F-010-text-fidelity.md)。

打开文件后，展开“文档诊断信息”，可查看 UTF-8 编码、开头 BOM、换行风格与计数、主换行风格和末尾换行。LF 与 CRLF 同时出现会显示混合，数量相同时显示并列；孤立 CR 单独计数。空文件与仅 BOM 文件没有换行，不会被推定为 LF。

元信息跟随当前成功读取的正文切换和刷新，不转换原文，不写回磁盘。无效 UTF-8 会拒绝读取并提示用外部工具转换，保留此前正文和文件集合。当前仍限单文件 1 MiB、只读原文；此功能不代表编辑和保存保真已实现，后续分别由 OBL-062 / OBL-063 承接。

开发验收样本可用 `bun scripts/create-f010-fixtures.ts` 生成；命令输出新建临时目录与每份样本的预期诊断，不修改用户文档。BOM 和 CRLF 等不可见字节主要由自动测试核对，人工验收查看诊断、切换和失败提示即可。

### F-011：常驻文档标签

已于 2026-09-22 通过用户人工验收，验收包为 `0.1.0-alpha.3`；仅记录用户结论，不扩写具体操作。构建及 Agent 实际交互证据与限制见 [F-011 迭代记录](iterations/F-011-persistent-document-tabs.md)。

| 操作 | 本轮行为 |
| --- | --- |
| 打开多个文件 | 主区顶部显示文件名与关闭按钮；常驻标签不会被随后打开的文件替换。 |
| 重复打开 | 已打开的同一文档直接激活，保留现有快照，不新增标签。 |
| 切换标签 | 直接展示该标签快照，不自动重读磁盘；当前运行期保留原文滚动位置。 |
| 重新读取 | 仅刷新当前标签；失败保留旧快照并提示，不影响其他标签。 |
| 关闭标签 | 点击标签关闭按钮、使用文件菜单/命令面板的“关闭当前标签”或按 `⌘W`；侧栏文件保留，磁盘文件不删除。 |
| 关闭活动标签 | 优先激活右邻，没有右邻则左邻；全部关闭后保留窗口及侧栏，可重新打开。 |
| 键盘访问 | `Option+Tab` 聚焦标签或关闭按钮（完整键盘导航启用时也可 Tab），Enter/空格执行；溢出标签栏可横向滚动。 |

与此前只读入口的区别：切换已打开文档不再重新读取磁盘；希望查看外部修改时使用“重新读取”。关闭标签与“清空窗口”不同，后者同时移除当前窗口文件集合。

仍仅支持单文件 1 MiB 以内的只读原文，不含编辑、保存、临时预览、标签排序/固定、分栏或重启恢复。“常驻”不代表跨重启持久化；后续模式、编辑状态及稳定锚点分别在对应功能实现。

## 待验收功能

### F-012a：内存编辑底座（待人工验收）

[迭代记录与人工步骤](iterations/F-012a-memory-editing-foundation.md)。Electrobun 2.0.1 前置迁移已验收；以下是新接入、尚未人工验收的行为。

复验包为 `0.1.0-alpha.8`：选中文字后切到另一标签再回来，即使正文尚未聚焦，也应看到选区高亮，不应为显示选区抢走当前焦点。Agent 已在最终包重复验证该路径，用户尚未复验；其他项目已由用户确认通过，整体及 OBL-062 仍未关闭。alpha.5 / alpha.7 的历史界面证据保留，不改写为 alpha.8 全部重测。

中文确认限制焦点、支持 Escape 取消并恢复原焦点；更新重启只做受控测试和接线，未实际安装远程更新。正常刷新已实际验证，但导航调用不等于导航完成；静默失败无法证明销毁时会保持冻结，不盲目恢复输入。强制终止、异常退出会丢失内存编辑，完整 IME 专项尚未验收。

| 操作 | 新增或变化 |
| --- | --- |
| 输入原文 | 正文改为 CM6 原文编辑验证入口，可输入、选择、复制粘贴；不是正式编辑模式或源码模式。界面提示“当前编辑尚不支持保存”。 |
| 切换或重复打开 | 保留各文档内存正文、选区、滚动与基础撤销记录；标签以“未保存”表示正文不同于读取基线。 |
| 撤销与重做 | 编辑区内使用 `⌘Z` / `⌘⇧Z`；撤销回读取基线时清除未保存标识。本轮仅基础验证，不代表完整 F-014。 |
| 文档诊断 | 分别显示磁盘快照和当前内存的 BOM、LF/CRLF、孤立 CR 与末尾属性，以及内存版本；仅观察，不转码。 |
| 关闭标签、重读、清空 | 有未保存变更时先询问，取消保留；明确放弃后执行。重读失败仍保留旧内存内容。 |
| 同路径被替换后重选 | 旧文档存在修改时先询问，不自动丢弃旧内存；取消不接纳新文件身份。 |
| 红灯、退出与刷新 | 汇总所有标签的修改，一次确认全部；取消后可继续编辑。确认及操作期间暂停编辑，通信失败不视为批准。 |
| 样本检查 | 运行 `bun scripts/create-f012-fixtures.ts` 创建一次性样本及 SHA256SUMS；测试前后校验磁盘未变。 |

仍限 1 MiB 验证范围，超限编辑拒绝而非截断。没有写盘、自动保存、正式 Markdown 渲染、完整输入法专项或重启恢复；批准放弃或异常退出会丢失内存修改。更新重启保护只验证受控接线，本轮不声称真实远程安装通过。OBL-062、父 F-012 和其余延期责任均未验收。

## 维护规则

- 每轮涉及功能新增或变更时，同步更新本手册的操作方式、可观察行为和限制，并链接对应迭代记录。
- 已实现但尚未人工验收的行为放在“待验收功能”中；变更已有功能时，明确指出受影响的已验收行为与待验收差异，不提前覆盖为已交付结论。
- 用户确认通过后，将本轮行为更新到“已验收功能”，补充验收日期，并清理对应待验收条目。继承草稿不得因入口可见而自动转为已验收。
- 行为调整或功能移除时，更新当前说明，并在下方保留简短变更记录；实现方案、命令输出和测试细节仍留在原迭代文档中。

## 变更记录

| 日期 | 功能 | 记录 |
| --- | --- | --- |
| 2026-09-22 | [F-012a](iterations/F-012a-memory-editing-foundation.md) | 用户确认除选区显示外其他项目通过；alpha.8 接入 CM6 drawSelection 修复失焦高亮，324 项测试及重复切换界面验证通过，仅该项待用户复验，整体与 OBL-062 未关闭。 |
| 2026-09-22 | [F-012a](iterations/F-012a-memory-editing-foundation.md) | alpha.7 最终构建、两套 Bun 全量 323 项测试及窗口回归通过，样本磁盘 SHA256 全部保持；待用户验收，OBL-062 与五项延期、父功能边界不变。保留 alpha.6 滚动回归失败及修复记录。 |
| 2026-09-22 | [F-012a](iterations/F-012a-memory-editing-foundation.md) | 内存编辑、逐文档状态、局部保真及放弃确认已接入，待人工验收；alpha.5 构建与阶段实测已记录，alpha.6 最终包及滚动修正回归待补。OBL-062 未关闭，父功能与五项延期不提前验收。 |
| 2026-09-22 | [F-012a 前置迁移](iterations/F-012a-memory-editing-foundation.md) | 用户确认“验证通过。进入下一轮迭代”，Electrobun 2.0.1 / alpha.4 迁移人工回归通过；不扩写操作证据，不代表编辑底座、未保存保护或 OBL-062 完成。 |
| 2026-09-22 | [F-012a 前置迁移](iterations/F-012a-memory-editing-foundation.md) | 升级 Electrobun 2.0.1，alpha.4 完成自动检查和基础运行回归，待人工回归验收；仍无编辑和保存，官方关闭保护仅在隔离验证程序测试。 |
| 2026-09-22 | [F-011](iterations/F-011-persistent-document-tabs.md) | 用户回复“验收通过”，常驻文档标签转入已验收，验收包 0.1.0-alpha.3；保留历史自动与实测边界，不追加用户具体操作。后续编辑、模式/锚点、恢复及缓存责任保留。 |
| 2026-09-22 | [F-011](iterations/F-011-persistent-document-tabs.md) | 常驻标签、同文档去重、快照切换及关闭重开已实现并自动验证，待人工验收；后续模式/编辑/恢复范围保留。 |
| 2026-09-22 | [F-010](iterations/F-010-text-fidelity.md) | 用户确认“验证通过”，读取保真范围转入已验收；保留既有自动验证记录，不扩写用户具体操作。OBL-062 / OBL-063 继续待承接，不自动开启下一轮或授权 Git 操作。 |
| 2026-09-22 | [F-010](iterations/F-010-text-fidelity.md) | 读取保真元信息与中文诊断完成实现和自动验证，验收包为 0.1.0-alpha.2，待人工验收；编辑与保存保真仍由后续功能承接。 |
| 2026-09-22 | [F-009a](iterations/F-009a-welcome-standalone-files.md) | 用户确认验收通过，切片转入已验收；保留 Agent 当时因 Mac 锁定未实测的记录，不扩写用户具体操作。F-009 父功能部分完成，OBL-039 / OBL-040 继续待承接。 |
| 2026-09-21 | [F-009a](iterations/F-009a-welcome-standalone-files.md) | 欢迎页、独立文件叶子、去重和只读切换完成实现与自动验证，待人工验收；真实新窗口检查受 Mac 锁定阻塞。欢迎页后续入口留在承接台账，不关闭完整父功能。 |
| 2026-09-21 | [L1 集中验收](iterations/L1-module-acceptance.md) | 用户确认“模块 1～6验证通过”，F-008a 转入已验收，方案 A 的 L1 基础范围完成；F-002 持续约束及后层责任保留，恢复逐轮选择，不授权提交或推送。 |
| 2026-09-21 | [F-008a](iterations/F-008a-shell-security-baseline.md) | 按方案 A 补齐 L1 当前外壳安全、匿名日志和设置版本基线；实现与自动验证完成，待集中按模块人工验收，后续消费者保留。 |
| 2026-09-21 | [F-005a](iterations/F-005a-document-task-cancellation.md) | 为当前只读入口加入后台取消、有界最新任务调度和提交检查，完成自动验证，待人工验收；父功能和缓存能力未完成。 |
| 2026-09-19 | [F-001](iterations/F-001-app-shell.md) | 建立手册，补录已验收的应用外壳、窗口操作、侧栏切换及离线启动；其余 starter 能力保持继承草稿。 |
| 2026-09-20 | [F-003a](iterations/F-003a-document-read-service.md) | 新增只读文档服务验证入口，替换正文占位内容；自动验证后进入待人工验收，不改变 F-001 已验收结论或提升完整 F-003 状态。 |
| 2026-09-20 | [F-003a](iterations/F-003a-document-read-service.md) | 用户确认六步基础交互与离线验收通过；剩余内容修改/版本变化人工检查待完成，切片仍待人工验收。 |
| 2026-09-20 | [F-003a](iterations/F-003a-document-read-service.md) | 用户确认修改后的内容已显示，截图验证新身份及旧快照提示清除；只读切片验收通过，完整 F-003 仍为部分完成。 |
| 2026-09-20 | [F-004a](iterations/F-004a-single-file-authorization.md) | 新增单文件路径识别与只读授权强化，完成自动验证并进入待人工验收；F-004 父功能未完成，F-003 仍为部分完成。 |
| 2026-09-20 | [F-004a](iterations/F-004a-single-file-authorization.md) | 用户明确确认验收通过，切片转入已验收功能；F-004 仍为部分完成，保留自动操作中通信失败的历史观察与未确认原因。 |
| 2026-09-20 | [F-006a](iterations/F-006a-command-entrypoints.md) | 统一现有五项命令的按钮、菜单、快捷键和命令面板入口及可用状态，完成自动验证并进入待人工验收；完整 F-006 与 F-007 未完成。 |
| 2026-09-20 | [F-006a](iterations/F-006a-command-entrypoints.md) | 用户确认“验证通过。进入下一轮”，切片转入已验收功能；F-006 仍为部分完成，不扩大输入法或离线验证证据。 |
| 2026-09-21 | [F-007a](iterations/F-007a-chinese-shell-theme.md) | 当前自有界面固定中文，移除语言选择，完善浅色/深色/跟随系统外观及偏好保存；实现与自动验证完成，待人工验收，不提升其他 starter 草稿或完整 F-007 状态。 |
| 2026-09-21 | [F-007a](iterations/F-007a-chinese-shell-theme.md) | 用户确认“验收通过”，本轮清单验收完成，切片转入已验收功能；F-007 仍为部分完成，开发阶段自动验证证据边界保持不变。 |
| 2026-09-21 | [F-005a](iterations/F-005a-document-task-cancellation.md) | 用户确认“验收通过”，切片转入已验收功能；F-005 仍为部分完成，取消竞态与有界调度继续以既有自动测试为证，不扩写人工操作或测量结果。 |
